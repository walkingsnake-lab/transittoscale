{
  "name": "transit-to-scale",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run data:build && vite build",
    "preview": "vite preview",
    "data:build": "node ./scripts/build-data.mjs",
    "data:check": "node ./scripts/check-data.mjs"
  },
  "dependencies": {
    "d3-geo": "^3.1.1"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
